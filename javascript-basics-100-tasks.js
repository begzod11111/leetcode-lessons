"use strict";

/**
 * 100 beginner JavaScript tasks.
 *
 * How to work:
 * 1. Solve tasks from top to bottom.
 * 2. Replace `return undefined;` with your own code.
 * 3. Add temporary console.log calls if you want to test a task manually.
 * 4. When a task works, send your solution for review and refactoring.
 *
 * The tasks intentionally do not contain ready answers.
 */

// 1. Return the number 100.
function task001ReturnNumber() {
  return undefined;
}

// 2. Return the string "JavaScript".
function task002ReturnString() {
  return undefined;
}

// 3. Return the boolean value true.
function task003ReturnBoolean() {
  return undefined;
}

// 4. Return null.
function task004ReturnNull() {
  return undefined;
}

// 5. Create a variable without assigning a value and return it.
function task005ReturnUndefined() {
  return undefined;
}

// 6. Return the type of the value using typeof.
function task006GetType(value) {
  return undefined;
}

// 7. Return true if value is a string.
function task007IsString(value) {
  return undefined;
}

// 8. Return true if value is a number.
function task008IsNumber(value) {
  return undefined;
}

// 9. Return true if value is null.
function task009IsNull(value) {
  return undefined;
}

// 10. Return true if value is undefined.
function task010IsUndefined(value) {
  return undefined;
}

// 11. Return the sum of a and b.
function task011Add(a, b) {
  return undefined;
}

// 12. Return the result of a minus b.
function task012Subtract(a, b) {
  return undefined;
}

// 13. Return the result of a multiplied by b.
function task013Multiply(a, b) {
  return undefined;
}

// 14. Return the result of a divided by b.
function task014Divide(a, b) {
  return undefined;
}

// 15. Return the remainder after a is divided by b.
function task015Remainder(a, b) {
  return undefined;
}

// 16. Return number squared.
function task016Square(number) {
  return undefined;
}

// 17. Return number cubed.
function task017Cube(number) {
  return undefined;
}

// 18. Return true if number is even.
function task018IsEven(number) {
  return undefined;
}

// 19. Return true if number is odd.
function task019IsOdd(number) {
  return undefined;
}

// 20. Return true if number is positive.
function task020IsPositive(number) {
  return undefined;
}

// 21. Return true if number is negative.
function task021IsNegative(number) {
  return undefined;
}

// 22. Return the bigger number.
function task022MaxOfTwo(a, b) {
  return undefined;
}

// 23. Return the smaller number.
function task023MinOfTwo(a, b) {
  return undefined;
}

// 24. Return 0 if number is negative, otherwise return number.
function task024ClampToZero(number) {
  return undefined;
}

// 25. Convert minutes to seconds.
function task025MinutesToSeconds(minutes) {
  return undefined;
}

// 26. Convert hours to seconds.
function task026HoursToSeconds(hours) {
  return undefined;
}

// 27. Return the area of a rectangle.
function task027RectangleArea(width, height) {
  return undefined;
}

// 28. Return the area of a triangle.
function task028TriangleArea(base, height) {
  return undefined;
}

// 29. Return the area of a circle.
function task029CircleArea(radius) {
  return undefined;
}

// 30. Return price rounded to 2 digits after the decimal point.
function task030RoundPrice(price) {
  return undefined;
}

// 31. Return percent percent of number.
function task031CalculatePercent(number, percent) {
  return undefined;
}

// 32. Return price with tax added. Tax is passed as percent.
function task032AddTax(price, taxPercent) {
  return undefined;
}

// 33. Return price after discount. Discount is passed as percent.
function task033ApplyDiscount(price, discountPercent) {
  return undefined;
}

// 34. Return true if number is between min and max, including min and max.
function task034IsInRange(number, min, max) {
  return undefined;
}

// 35. Return the last digit of a positive integer.
function task035GetLastDigit(number) {
  return undefined;
}

// 36. Return the length of text.
function task036StringLength(text) {
  return undefined;
}

// 37. Return the first character of text.
function task037FirstChar(text) {
  return undefined;
}

// 38. Return the last character of text.
function task038LastChar(text) {
  return undefined;
}

// 39. Return text in uppercase.
function task039UpperCase(text) {
  return undefined;
}

// 40. Return text in lowercase.
function task040LowerCase(text) {
  return undefined;
}

// 41. Return text without spaces at the beginning and end.
function task041TrimText(text) {
  return undefined;
}

// 42. Return true if text contains word.
function task042ContainsWord(text, word) {
  return undefined;
}

// 43. Return true if text starts with prefix.
function task043StartsWith(text, prefix) {
  return undefined;
}

// 44. Return true if text ends with suffix.
function task044EndsWith(text, suffix) {
  return undefined;
}

// 45. Return word repeated count times.
function task045RepeatWord(word, count) {
  return undefined;
}

// 46. Return firstName and lastName joined with one space.
function task046FullName(firstName, lastName) {
  return undefined;
}

// 47. Return the initials from firstName and lastName.
function task047Initials(firstName, lastName) {
  return undefined;
}

// 48. Replace the first oldWord in text with newWord.
function task048ReplaceWord(text, oldWord, newWord) {
  return undefined;
}

// 49. Return the number of spaces in text.
function task049CountSpaces(text) {
  return undefined;
}

// 50. Return text reversed.
function task050ReverseString(text) {
  return undefined;
}

// 51. Return true if text is the same forward and backward.
function task051IsPalindrome(text) {
  return undefined;
}

// 52. Return the middle character of text. If length is even, return two middle characters.
function task052MiddleCharacters(text) {
  return undefined;
}

// 53. Return text with the first character uppercase and the rest unchanged.
function task053CapitalizeFirst(text) {
  return undefined;
}

// 54. Return a simple slug: lowercase text where spaces are replaced with hyphens.
function task054MakeSlug(text) {
  return undefined;
}

// 55. Return email with everything before @ replaced by "***".
function task055MaskEmail(email) {
  return undefined;
}

// 56. Return an array that contains a, b, and c.
function task056CreateArray(a, b, c) {
  return undefined;
}

// 57. Return the length of array.
function task057ArrayLength(array) {
  return undefined;
}

// 58. Return the first item of array.
function task058FirstItem(array) {
  return undefined;
}

// 59. Return the last item of array.
function task059LastItem(array) {
  return undefined;
}

// 60. Return a new array with item added to the end.
function task060AddToEnd(array, item) {
  return undefined;
}

// 61. Return a new array with item added to the beginning.
function task061AddToStart(array, item) {
  return undefined;
}

// 62. Return a new array without the last item.
function task062RemoveLast(array) {
  return undefined;
}

// 63. Return a new array without the first item.
function task063RemoveFirst(array) {
  return undefined;
}

// 64. Return array items joined by separator.
function task064JoinArray(array, separator) {
  return undefined;
}

// 65. Return true if array includes item.
function task065IncludesItem(array, item) {
  return undefined;
}

// 66. Return the index of item in array.
function task066FindIndex(array, item) {
  return undefined;
}

// 67. Return a new array where every number is doubled.
function task067DoubleNumbers(numbers) {
  return undefined;
}

// 68. Return a new array where every number is squared.
function task068SquareNumbers(numbers) {
  return undefined;
}

// 69. Return only even numbers.
function task069FilterEvenNumbers(numbers) {
  return undefined;
}

// 70. Return only positive numbers.
function task070FilterPositiveNumbers(numbers) {
  return undefined;
}

// 71. Return the sum of all numbers.
function task071SumNumbers(numbers) {
  return undefined;
}

// 72. Return the average of all numbers.
function task072AverageNumbers(numbers) {
  return undefined;
}

// 73. Return the biggest number from the array.
function task073MaxNumber(numbers) {
  return undefined;
}

// 74. Return the smallest number from the array.
function task074MinNumber(numbers) {
  return undefined;
}

// 75. Return how many times item appears in array.
function task075CountOccurrences(array, item) {
  return undefined;
}

// 76. Return a new array without false, 0, "", null, undefined, and NaN.
function task076RemoveFalsyValues(array) {
  return undefined;
}

// 77. Return a new array with items in reverse order.
function task077ReverseArray(array) {
  return undefined;
}

// 78. Return numbers sorted from smallest to biggest.
function task078SortNumbersAsc(numbers) {
  return undefined;
}

// 79. Return strings sorted alphabetically.
function task079SortStrings(strings) {
  return undefined;
}

// 80. Return a new array with duplicate items removed.
function task080UniqueItems(array) {
  return undefined;
}

// 81. Return an object with name and age properties.
function task081CreateUser(name, age) {
  return undefined;
}

// 82. Return user.name.
function task082GetUserName(user) {
  return undefined;
}

// 83. Return a new user object with age changed to newAge.
function task083UpdateUserAge(user, newAge) {
  return undefined;
}

// 84. Return true if object has the given property.
function task084HasProperty(object, property) {
  return undefined;
}

// 85. Return the number of keys in object.
function task085CountKeys(object) {
  return undefined;
}

// 86. Return an array of object values.
function task086ObjectValues(object) {
  return undefined;
}

// 87. Return a new object that contains properties from first and second.
function task087MergeObjects(first, second) {
  return undefined;
}

// 88. Return a product object with title, price, and quantity.
function task088CreateProduct(title, price, quantity) {
  return undefined;
}

// 89. Return product price multiplied by product quantity.
function task089ProductTotal(product) {
  return undefined;
}

// 90. Return a string with user city and street, separated by comma and space.
function task090FullAddress(user) {
  return undefined;
}

// 91. Return an array of names from an array of users.
function task091UserNames(users) {
  return undefined;
}

// 92. Return only users where isActive is true.
function task092ActiveUsers(users) {
  return undefined;
}

// 93. Return the total price of all products in cart.
function task093CartTotal(cart) {
  return undefined;
}

// 94. Return the user with the matching id.
function task094FindUserById(users, id) {
  return undefined;
}

// 95. Return an object where keys are letters and values are counts.
function task095CountLetters(text) {
  return undefined;
}

// 96. Return one array made from an array of arrays.
function task096FlattenOneLevel(arrays) {
  return undefined;
}

// 97. Return the longest string from an array of strings.
function task097LongestString(strings) {
  return undefined;
}

// 98. Return true if password has at least 8 characters and contains a number.
function task098ValidatePassword(password) {
  return undefined;
}

// 99. Return text trimmed, lowercased, and with repeated spaces changed to one space.
function task099NormalizeText(text) {
  return undefined;
}

// 100. Return a new user object with trimmed name and lowercased email.
function task100NormalizeUser(user) {
  return undefined;
}

module.exports = {
  task001ReturnNumber,
  task002ReturnString,
  task003ReturnBoolean,
  task004ReturnNull,
  task005ReturnUndefined,
  task006GetType,
  task007IsString,
  task008IsNumber,
  task009IsNull,
  task010IsUndefined,
  task011Add,
  task012Subtract,
  task013Multiply,
  task014Divide,
  task015Remainder,
  task016Square,
  task017Cube,
  task018IsEven,
  task019IsOdd,
  task020IsPositive,
  task021IsNegative,
  task022MaxOfTwo,
  task023MinOfTwo,
  task024ClampToZero,
  task025MinutesToSeconds,
  task026HoursToSeconds,
  task027RectangleArea,
  task028TriangleArea,
  task029CircleArea,
  task030RoundPrice,
  task031CalculatePercent,
  task032AddTax,
  task033ApplyDiscount,
  task034IsInRange,
  task035GetLastDigit,
  task036StringLength,
  task037FirstChar,
  task038LastChar,
  task039UpperCase,
  task040LowerCase,
  task041TrimText,
  task042ContainsWord,
  task043StartsWith,
  task044EndsWith,
  task045RepeatWord,
  task046FullName,
  task047Initials,
  task048ReplaceWord,
  task049CountSpaces,
  task050ReverseString,
  task051IsPalindrome,
  task052MiddleCharacters,
  task053CapitalizeFirst,
  task054MakeSlug,
  task055MaskEmail,
  task056CreateArray,
  task057ArrayLength,
  task058FirstItem,
  task059LastItem,
  task060AddToEnd,
  task061AddToStart,
  task062RemoveLast,
  task063RemoveFirst,
  task064JoinArray,
  task065IncludesItem,
  task066FindIndex,
  task067DoubleNumbers,
  task068SquareNumbers,
  task069FilterEvenNumbers,
  task070FilterPositiveNumbers,
  task071SumNumbers,
  task072AverageNumbers,
  task073MaxNumber,
  task074MinNumber,
  task075CountOccurrences,
  task076RemoveFalsyValues,
  task077ReverseArray,
  task078SortNumbersAsc,
  task079SortStrings,
  task080UniqueItems,
  task081CreateUser,
  task082GetUserName,
  task083UpdateUserAge,
  task084HasProperty,
  task085CountKeys,
  task086ObjectValues,
  task087MergeObjects,
  task088CreateProduct,
  task089ProductTotal,
  task090FullAddress,
  task091UserNames,
  task092ActiveUsers,
  task093CartTotal,
  task094FindUserById,
  task095CountLetters,
  task096FlattenOneLevel,
  task097LongestString,
  task098ValidatePassword,
  task099NormalizeText,
  task100NormalizeUser,
};
